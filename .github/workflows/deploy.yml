name: Deploy

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

env:
  STACK_BASE_NAME: options-advisor

jobs:
  deploy-dev:
    name: Deploy — dev
    runs-on: ubuntu-latest
    environment: dev
    outputs:
      api-url: ${{ steps.outputs.outputs.api-url }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Install dependencies
        run: npm ci

      - name: Sync secrets to AWS
        run: |
          upsert_secret() {
            local name="$1" value="$2"
            if aws secretsmanager describe-secret --secret-id "$name" > /dev/null 2>&1; then
              aws secretsmanager put-secret-value --secret-id "$name" --secret-string "$value"
            else
              aws secretsmanager create-secret --name "$name" --secret-string "$value"
            fi
          }
          upsert_secret "/options-advisor/dev/finnhub-api-key"    "${{ secrets.FINHUB_API_KEY }}"
          upsert_secret "/options-advisor/dev/flash-alpha-api-key" "${{ secrets.FLASH_ALPHA_API_KEY }}"

      - name: Bootstrap CDK (idempotent)
        run: npx cdk bootstrap

      - name: Deploy dev stack
        run: |
          npx cdk deploy "$STACK_BASE_NAME-dev" \
            --require-approval never \
            -c stackType=dev \
            --outputs-file cdk-outputs.json

      - name: Extract API URL
        id: outputs
        run: echo "api-url=$(jq -r '.[keys[0]].ApiUrl' cdk-outputs.json)" >> "$GITHUB_OUTPUT"

  acceptance-test-dev:
    name: Acceptance tests — dev
    needs: deploy-dev
    runs-on: ubuntu-latest
    environment: dev

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Install dependencies
        run: npm ci

      - name: Seed watchlist
        run: ./scripts/seed-watchlist.sh dev
        env:
          AWS_DEFAULT_REGION: us-east-1

      - name: Run acceptance tests
        run: npm run test:acceptance
        env:
          STAGE: dev
          AWS_REGION: us-east-1

  deploy-prod:
    name: Deploy — production
    needs: acceptance-test-dev
    runs-on: ubuntu-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Install dependencies
        run: npm ci

      - name: Sync secrets to AWS
        run: |
          upsert_secret() {
            local name="$1" value="$2"
            if aws secretsmanager describe-secret --secret-id "$name" > /dev/null 2>&1; then
              aws secretsmanager put-secret-value --secret-id "$name" --secret-string "$value"
            else
              aws secretsmanager create-secret --name "$name" --secret-string "$value"
            fi
          }
          upsert_secret "/options-advisor/production/finnhub-api-key"    "${{ secrets.FINHUB_API_KEY }}"
          upsert_secret "/options-advisor/production/flash-alpha-api-key" "${{ secrets.FLASH_ALPHA_API_KEY }}"

      - name: Bootstrap CDK (idempotent)
        run: npx cdk bootstrap

      - name: Deploy production stack
        run: |
          npx cdk deploy "$STACK_BASE_NAME" \
            --require-approval never \
            -c stackType=prod
